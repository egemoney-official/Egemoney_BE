spring:
  application:
    name: igemoney_BE
  profiles:
    default: dev

  jpa:
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true

app:
  costume:
    public-url-prefix: /costumes/
  auth:
    admin-ids:
      - 4474468638

jwt:
  secret: ${JWT_SECRET_KEY:this_is_dummy_jwt_secret_key_not_for_dev_but_ci_passing_lessssssssssgo}
  expiration: 3600000 # 1h

logging:
  pattern:
    dateformat: "yyyy-MM-dd HH:mm:ss.SSS, Asia/Seoul"


kakao:
  client-id: ${KAKAO_CLIENT_ID:bdbd4b13fec8e8fb48a6f4386b9503f4}
  redirect-uri: ${KAKAO_REDIRECT_URI:http://localhost:8080}

springdoc:
  swagger-ui:
    path: /api-test
    groups-order: DESC
    operations-sorter: alpha
  paths-to-match:
    - /api/**
